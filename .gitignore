# Binaries
/osapi
/dist/
*.exe
*.test
*.out
/internal/apispec/gen/gen

# Go
/vendor/
coverage.txt
coverage.html

# Editors / OS
.vscode/
.idea/
.DS_Store

# Local plan/scratch (design docs with local paths — not for the public repo)
/PLAN.md

# Vendored spec fetched on demand by `make update-spec`; only paths_gen.go is committed
/internal/apispec/spec/opensearch-openapi.yaml
